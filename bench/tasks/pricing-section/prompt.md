Build a responsive pricing section (a single self-contained `index.html` with inline CSS, no
frameworks) with three tiers: Starter, Pro, and Enterprise.

Each tier card shows the plan name, a price, a short list of included features, and a sign-up
button. Highlight the Pro plan as the recommended one. It should look polished and lay out
sensibly on mobile.

Put the whole thing in one HTML code block.
