Build a centered sign-up form (a single self-contained `index.html` with inline CSS, no
frameworks) with fields for full name, email, and password, plus a submit button.

It should look clean and modern and work on mobile.

Put the whole thing in one HTML code block.
