Build a landing page for "Honeycomb", a SaaS tool that auto-generates API documentation from
your codebase.

Single self-contained `index.html` with inline CSS (no external files or frameworks). Include:
a sticky top nav with the product name and a call-to-action button; a hero section with a
headline, subhead, and primary CTA; a features section with three feature cards; a simple
pricing section; and a footer.

Put the whole page in one HTML code block.
