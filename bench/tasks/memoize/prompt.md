Write a JavaScript function `memoize(fn)` that returns a memoized version of `fn`: repeated
calls with the same arguments return a cached result instead of recomputing. Assume arguments
are JSON-serializable. Different argument lists are cached separately.

Put it in a single JavaScript code block and `module.exports = memoize` (or `{ memoize }`).
